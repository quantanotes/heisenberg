syntax = "proto3";

option go_package="heisenberg/internal/store";

package store;

message Pong {
    string type = "HEISENBERG_STORE";
}

service Query {
    rpc Ping() return (Pong) {}
    rpc Get() returns () {}
}
